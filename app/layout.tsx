import React from "react"
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'ZiaanHub - Roblox Exploit Script',
  description: 'ZiaanHub adalah script exploit untuk Roblox yang support Mobile dan PC. Download dan gunakan script kami sekarang.',
  generator: 'v0.app',
  icons: {
    icon: '/ziaanhub-logo.png',
    apple: '/ziaanhub-logo.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
